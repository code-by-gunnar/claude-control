---
phase: 09-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/components/EmptyState.tsx
  - dashboard/src/pages/SettingsPage.tsx
  - dashboard/src/pages/MemoryPage.tsx
  - dashboard/src/pages/McpPage.tsx
  - dashboard/src/pages/PluginsPage.tsx
  - dashboard/src/pages/HooksPage.tsx
  - dashboard/src/pages/AgentsPage.tsx
  - dashboard/src/pages/SkillsPage.tsx
  - dashboard/src/pages/PermissionsPage.tsx
  - dashboard/src/pages/MarketplacesPage.tsx
  - dashboard/src/pages/ProjectsPage.tsx
  - dashboard/src/pages/HealthPage.tsx
  - dashboard/src/pages/OverviewPage.tsx
autonomous: true

must_haves:
  truths:
    - "Every dashboard page shows helpful guidance when no data exists for that config type"
    - "Empty states explain WHY nothing is configured (not bare 'No X found')"
    - "Empty states suggest WHAT to do next with specific actions (file paths, commands)"
    - "Overview page displays subscription type and rate limit tier alongside stat cards"
  artifacts:
    - path: "dashboard/src/components/EmptyState.tsx"
      provides: "Reusable empty state component with icon, title, description, and action hint"
      min_lines: 20
    - path: "dashboard/src/pages/OverviewPage.tsx"
      provides: "Account info section with subscription type and rate limit tier"
      contains: "fetchAccount"
  key_links:
    - from: "dashboard/src/pages/*.tsx"
      to: "dashboard/src/components/EmptyState.tsx"
      via: "import and render when data array is empty"
      pattern: "EmptyState"
    - from: "dashboard/src/pages/OverviewPage.tsx"
      to: "/api/account"
      via: "fetchAccount() in useEffect"
      pattern: "fetchAccount"
---

<objective>
Add contextual empty states to every dashboard page and surface account info on the overview.

Purpose: New users see blank pages with unhelpful "No X found" messages. This phase replaces those with guidance that explains WHY nothing exists and WHAT to do about it. Also surfaces subscription and rate limit info for quick reference.
Output: Reusable EmptyState component, enhanced empty states on all pages, account info on overview.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-layout/08-01-SUMMARY.md

@dashboard/src/components/EmptyState.tsx
@dashboard/src/pages/OverviewPage.tsx
@dashboard/src/pages/SettingsPage.tsx
@dashboard/src/pages/MemoryPage.tsx
@dashboard/src/pages/McpPage.tsx
@dashboard/src/pages/PluginsPage.tsx
@dashboard/src/pages/HooksPage.tsx
@dashboard/src/pages/AgentsPage.tsx
@dashboard/src/pages/SkillsPage.tsx
@dashboard/src/pages/PermissionsPage.tsx
@dashboard/src/pages/MarketplacesPage.tsx
@dashboard/src/pages/ProjectsPage.tsx
@dashboard/src/pages/HealthPage.tsx
@dashboard/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable EmptyState component</name>
  <files>dashboard/src/components/EmptyState.tsx</files>
  <action>
    Create a reusable EmptyState component that accepts:
    - `icon`: An inline SVG element (Heroicons style, consistent with existing Icon component usage)
    - `title`: string — what's missing (e.g., "No MCP servers configured")
    - `description`: string — WHY nothing is there and context
    - `action`: string — WHAT to do next (file path, command, or instruction)

    Layout: Centered vertically in a white rounded card (matching existing `bg-white rounded-lg shadow-sm border border-slate-200` pattern), with:
    - Icon at top (slate-300, 48px)
    - Title below (text-lg font-medium text-slate-500)
    - Description paragraph (text-sm text-slate-400, max-w-md, centered)
    - Action hint at bottom with monospace styling for paths/commands (text-sm text-slate-400, with code spans using `bg-slate-100 px-1.5 py-0.5 rounded text-xs font-mono text-slate-500`)

    Use the same padding/spacing as existing empty states (p-8) for visual consistency.
    Export as named export `EmptyState`.
  </action>
  <verify>npm run build succeeds with no TypeScript errors</verify>
  <done>EmptyState component exported and importable from dashboard/src/components/EmptyState.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Replace all page empty states with contextual EmptyState</name>
  <files>
    dashboard/src/pages/SettingsPage.tsx,
    dashboard/src/pages/MemoryPage.tsx,
    dashboard/src/pages/McpPage.tsx,
    dashboard/src/pages/PluginsPage.tsx,
    dashboard/src/pages/HooksPage.tsx,
    dashboard/src/pages/AgentsPage.tsx,
    dashboard/src/pages/SkillsPage.tsx,
    dashboard/src/pages/PermissionsPage.tsx,
    dashboard/src/pages/MarketplacesPage.tsx,
    dashboard/src/pages/ProjectsPage.tsx,
    dashboard/src/pages/HealthPage.tsx
  </files>
  <action>
    Replace each page's bare empty-state div with the new EmptyState component. Each gets a contextual message:

    **SettingsPage** (when no settings and no filter active):
    - title: "No settings configured"
    - description: "Claude Code uses default settings until you customize them."
    - action: "Create ~/.claude/settings.json or .claude/settings.json in your project"

    **MemoryPage** (when files.length === 0):
    - title: "No CLAUDE.md files found"
    - description: "CLAUDE.md files give Claude persistent instructions and context about your project or preferences."
    - action: "Create ~/.claude/CLAUDE.md for global instructions or CLAUDE.md in your project root"

    **McpPage** (when servers.length === 0):
    - title: "No MCP servers configured"
    - description: "MCP servers extend Claude with external tools and data sources like databases, APIs, and file systems."
    - action: "Add servers in ~/.claude/settings.json under the mcpServers key"

    **PluginsPage** (when plugins.length === 0):
    - title: "No plugins installed"
    - description: "Plugins add pre-built capabilities from the Claude marketplace to your Claude Code setup."
    - action: "Run /install-plugin in Claude Code to browse and install plugins"

    **HooksPage** (when allEvents.length === 0):
    - title: "No hooks configured"
    - description: "Hooks run shell commands when Claude Code events occur, like after editing files or before committing."
    - action: "Add hooks in settings.json under the hooks key"

    **AgentsPage** (when agents.length === 0):
    - title: "No custom agents found"
    - description: "Agent files define specialized Claude behaviors with custom system prompts, tool sets, and model preferences."
    - action: "Create .md files in ~/.claude/agents/ with YAML frontmatter"

    **SkillsPage** (when allCustomCommands.length === 0, in the Custom Commands section):
    - title: "No custom commands configured"
    - description: "Custom commands are slash commands you define for frequently used prompts or workflows."
    - action: "Create command files in ~/.claude/commands/ or .claude/commands/"

    **PermissionsPage** (when permissions.length === 0):
    - title: "No permission rules configured"
    - description: "Permission rules control which tools Claude can run automatically, with prompting, or not at all."
    - action: "Add rules in settings.json under the permissions key with allow, deny, or ask arrays"

    **MarketplacesPage** (when marketplaces.length === 0):
    - title: "No marketplaces found"
    - description: "Marketplaces are curated collections of plugins for Claude Code."
    - action: "Marketplaces are configured automatically when you install plugins"

    **ProjectsPage** (when projects.length === 0 in workspace discovery):
    - title: "No Claude Code projects found"
    - description: "Project discovery scans the parent directory for folders that contain Claude Code configuration."
    - action: "Make sure sibling directories have .claude/ folders or CLAUDE.md files"

    **HealthPage** — only add empty state if health data returns with zero checks (unlikely but handle gracefully):
    - title: "No health data available"
    - description: "Health scoring analyzes your Claude Code configuration for completeness and best practices."
    - action: "Ensure you have at least one configuration file (settings.json, CLAUDE.md, etc.)"

    For the SettingsPage, keep the existing filter-specific message ("No settings matching X") — only replace the no-data-at-all case.

    Use an appropriate Heroicons outline SVG for each (e.g., cog for settings, document for memory, server-stack for MCP, puzzle for plugins, bolt for hooks, user-group for agents, command-line for skills, shield for permissions, building-storefront for marketplaces, folder for projects, heart for health). Use the same inline SVG pattern already established in the codebase.
  </action>
  <verify>npm run build succeeds with no TypeScript errors</verify>
  <done>All pages show contextual empty states with title, description explaining why, and action explaining what to do next</done>
</task>

<task type="auto">
  <name>Task 3: Add account info section to OverviewPage</name>
  <files>dashboard/src/pages/OverviewPage.tsx</files>
  <action>
    Add an account info section to OverviewPage that displays subscription type and rate limit tier.

    Implementation:
    1. Import and call `fetchAccount()` from api.ts alongside the existing `fetchStatus()` call (parallel with Promise.all or separate useEffect)
    2. Display account info as a compact card/banner ABOVE the stat cards grid, with:
       - "Account" label on the left
       - Subscription type badge (e.g., "pro", "free", "max") — use colored badge similar to scope badges elsewhere
       - Rate limit tier badge (e.g., "tier1", "tier2")
       - If both are null (credentials not found), show a subtle message: "Account info unavailable — sign in to Claude Code to see subscription details"
    3. Style: Use a horizontal bar/card matching the existing dashboard aesthetic (bg-white rounded-lg shadow-sm border border-slate-200). Keep it compact — one line, not a large card.
    4. Handle loading state: show a small skeleton placeholder while fetching
    5. Handle error state: silently fall back to "unavailable" message (don't show red error for account — it's not critical)

    The fetchAccount() function and AccountInfo type already exist in api.ts. The /api/account endpoint already exists and returns { subscriptionType, rateLimitTier }.

    Badge colors for subscription type:
    - "pro" → blue (bg-blue-100 text-blue-700)
    - "max_5" or anything with "max" → purple (bg-purple-100 text-purple-700)
    - "free" or null → gray (bg-slate-100 text-slate-500)
    - Fallback: gray for unknown values

    Badge colors for rate limit tier:
    - Display as-is with neutral styling (bg-slate-100 text-slate-600)
  </action>
  <verify>npm run build succeeds with no TypeScript errors</verify>
  <done>Overview page shows subscription type and rate limit tier prominently above stat cards, handles missing credentials gracefully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm test` passes all existing tests
- [ ] EmptyState component renders with icon, title, description, action
- [ ] All pages show contextual guidance when data is empty (not bare "No X found")
- [ ] OverviewPage shows account info when credentials exist
- [ ] OverviewPage gracefully handles missing credentials
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every page has contextual empty state explaining why + what to do (GUIDE-01, GUIDE-02)
- Overview page shows subscription type and rate limit tier (GUIDE-04)
  </success_criteria>

<output>
After completion, create `.planning/phases/09-empty-states/09-01-SUMMARY.md`
</output>
